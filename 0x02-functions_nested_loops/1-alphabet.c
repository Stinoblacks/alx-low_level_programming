#include "main.h"

 /**
 * print_alphabet - print alphabet in lower case
 *
 * Return: always 0 (success)
 */

void print_alphabet(void)
{
char la;

for (la = 'a'; la <= 'z'; la++)
{
_putchar(la);
}
_putchar('\n);
}
