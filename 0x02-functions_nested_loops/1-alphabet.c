#include "main.h"

 /**
 * print_alphabet - print alphabet in lower case
 *
 * Return: always 0 (success)
 */

void print_alphabet(void)
{
int la;

for (la = 97; la <= 122; la++)
{
_putchar(la);
}
_putchar('\n);
}
